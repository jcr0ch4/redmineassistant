"""Identidade e versão do aplicativo — fonte única de verdade.

Alterar APP_VERSAO aqui ao lançar uma nova versão. Os mesmos valores são
usados pela UI (aba Configuração -> "Sobre"), pelo `--version` da linha de
comando e pelo `version_info.txt` (metadados do executável Windows).
"""

APP_NOME = "Redmine Assistant"
APP_EXECUTAVEL = "RedmineAssitant"
APP_VERSAO = "1.0.0"
APP_VERSAO_DISPLAY = f"{APP_NOME} v{APP_VERSAO}"